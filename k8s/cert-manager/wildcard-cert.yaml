apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: score-api-cert
  namespace: tetris
spec:
  secretName: score-api-tls
  issuerRef:
    name: aks-clusterissuer
    kind: ClusterIssuer
  commonName: api.<your-domain>
  dnsNames:
    - api.<your-domain>

